import { ComponentSourceData } from '../types';

export const fullscreentoggleSource: ComponentSourceData = {
  component: 'Fullscreen Toggle',
  slug: 'fullscreen-toggle',
  category: 'Extended UI',
  categorySlug: 'extended-ui',
  description: 'Button to toggle browser fullscreen mode with icon state transition.',
  sources: {
    react: {
      language: 'tsx',
      filename: 'FullscreenToggle.tsx',
      code: `import React, { useState } from 'react';
import { motion } from 'framer-motion';

export default function FullscreenToggle() {
  const [isActive, setIsActive] = useState(false);

  return (
    <motion.div
      initial={{ opacity: 0, y: 10 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.3 }}
      className="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800"
    >
      <p className="text-sm text-gray-700 dark:text-gray-300">Button to toggle browser fullscreen mode with icon state transition.</p>
      <button
        onClick={() => setIsActive(!isActive)}
        className="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600"
      >
        Toggle State
      </button>
    </motion.div>
  );
}`,
    },
    nextjs: {
      language: 'tsx',
      filename: 'fullscreen-toggle/page.tsx',
      code: `'use client';

import FullscreenToggle from '@/components/mtverse/extended-ui/FullscreenToggle';

export default function FullscreenTogglePage() {
  return (
    <div className="p-6">
      <FullscreenToggle />
    </div>
  );
}`,
    },
    vue: {
      language: 'vue',
      filename: 'FullscreenToggle.vue',
      code: `<template>
  <div class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
    <p class="text-sm text-gray-700 dark:text-gray-300">Button to toggle browser fullscreen mode with icon state transition.</p>
    <button
      @click="isActive = !isActive"
      class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600"
    >
      Toggle State
    </button>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue';
const isActive = ref(false);
</script>`,
    },
    angular: {
      language: 'ts',
      filename: 'fullscreen-toggle.component.ts',
      code: `import { Component } from '@angular/core';

@Component({
  selector: 'app-fullscreen-toggle',
  template: \`
    <div class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
      <p class="text-sm text-gray-700 dark:text-gray-300">Button to toggle browser fullscreen mode with icon state transition.</p>
      <button (click)="toggle()"
        class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
        Toggle State
      </button>
    </div>
  \`
})
export class FullscreenToggleComponent {
  isActive = false;
  toggle() { this.isActive = !this.isActive; }
}`,
    },
    html: {
      language: 'html',
      filename: 'fullscreen-toggle.html',
      code: `<!-- HTML + Alpine.js -->
<div x-data="{ isActive: false }" class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
  <p class="text-sm text-gray-700 dark:text-gray-300">Button to toggle browser fullscreen mode with icon state transition.</p>
  <button
    @click="isActive = !isActive"
    class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
    Toggle State
  </button>
</div>`,
    },
    laravel: {
      language: 'blade',
      filename: 'fullscreen-toggle.blade.php',
      code: `<!-- Laravel Blade + Alpine.js -->
<div x-data="{ isActive: false }" class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
  <p class="text-sm text-gray-700 dark:text-gray-300">Button to toggle browser fullscreen mode with icon state transition.</p>
  <button
    @click="isActive = !isActive"
    class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
    Toggle State
  </button>
</div>`,
    },
  },
};
