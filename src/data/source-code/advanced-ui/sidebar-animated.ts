import { ComponentSourceData } from '../types';

export const sidebaranimatedSource: ComponentSourceData = {
  component: 'Animated Sidebar',
  slug: 'sidebar-animated',
  category: 'Advanced UI',
  categorySlug: 'advanced-ui',
  description: 'Sidebar with smooth expand/collapse animation and content reflow.',
  sources: {
    react: {
      language: 'tsx',
      filename: 'SidebarAnimated.tsx',
      code: `import React, { useState } from 'react';
import { motion } from 'framer-motion';

export default function SidebarAnimated() {
  const [isActive, setIsActive] = useState(false);

  return (
    <motion.div
      initial={{ opacity: 0, y: 10 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.3 }}
      className="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800"
    >
      <p className="text-sm text-gray-700 dark:text-gray-300">Sidebar with smooth expand/collapse animation and content reflow.</p>
      <button
        onClick={() => setIsActive(!isActive)}
        className="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600"
      >
        Toggle State
      </button>
    </motion.div>
  );
}`,
    },
    nextjs: {
      language: 'tsx',
      filename: 'sidebar-animated/page.tsx',
      code: `'use client';

import SidebarAnimated from '@/components/mtverse/advanced-ui/SidebarAnimated';

export default function SidebarAnimatedPage() {
  return (
    <div className="p-6">
      <SidebarAnimated />
    </div>
  );
}`,
    },
    vue: {
      language: 'vue',
      filename: 'SidebarAnimated.vue',
      code: `<template>
  <div class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
    <p class="text-sm text-gray-700 dark:text-gray-300">Sidebar with smooth expand/collapse animation and content reflow.</p>
    <button
      @click="isActive = !isActive"
      class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600"
    >
      Toggle State
    </button>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue';
const isActive = ref(false);
</script>`,
    },
    angular: {
      language: 'ts',
      filename: 'sidebar-animated.component.ts',
      code: `import { Component } from '@angular/core';

@Component({
  selector: 'app-sidebar-animated',
  template: \`
    <div class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
      <p class="text-sm text-gray-700 dark:text-gray-300">Sidebar with smooth expand/collapse animation and content reflow.</p>
      <button (click)="toggle()"
        class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
        Toggle State
      </button>
    </div>
  \`
})
export class SidebarAnimatedComponent {
  isActive = false;
  toggle() { this.isActive = !this.isActive; }
}`,
    },
    html: {
      language: 'html',
      filename: 'sidebar-animated.html',
      code: `<!-- HTML + Alpine.js -->
<div x-data="{ isActive: false }" class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
  <p class="text-sm text-gray-700 dark:text-gray-300">Sidebar with smooth expand/collapse animation and content reflow.</p>
  <button
    @click="isActive = !isActive"
    class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
    Toggle State
  </button>
</div>`,
    },
    laravel: {
      language: 'blade',
      filename: 'sidebar-animated.blade.php',
      code: `<!-- Laravel Blade + Alpine.js -->
<div x-data="{ isActive: false }" class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
  <p class="text-sm text-gray-700 dark:text-gray-300">Sidebar with smooth expand/collapse animation and content reflow.</p>
  <button
    @click="isActive = !isActive"
    class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
    Toggle State
  </button>
</div>`,
    },
  },
};
