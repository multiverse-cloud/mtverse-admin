import { ComponentSourceData } from '../types';

export const infinitescrollloaderSource: ComponentSourceData = {
  component: 'Infinite Scroll Loader',
  slug: 'infinite-scroll-loader',
  category: 'Advanced UI',
  categorySlug: 'advanced-ui',
  description: 'Loading indicator for infinite scroll with pulse animation and scroll detection.',
  sources: {
    react: {
      language: 'tsx',
      filename: 'InfiniteScrollLoader.tsx',
      code: `import React, { useState } from 'react';
import { motion } from 'framer-motion';

export default function InfiniteScrollLoader() {
  const [isActive, setIsActive] = useState(false);

  return (
    <motion.div
      initial={{ opacity: 0, y: 10 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.3 }}
      className="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800"
    >
      <p className="text-sm text-gray-700 dark:text-gray-300">Loading indicator for infinite scroll with pulse animation and scroll detection.</p>
      <button
        onClick={() => setIsActive(!isActive)}
        className="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600"
      >
        Toggle State
      </button>
    </motion.div>
  );
}`,
    },
    nextjs: {
      language: 'tsx',
      filename: 'infinite-scroll-loader/page.tsx',
      code: `'use client';

import InfiniteScrollLoader from '@/components/mtverse/advanced-ui/InfiniteScrollLoader';

export default function InfiniteScrollLoaderPage() {
  return (
    <div className="p-6">
      <InfiniteScrollLoader />
    </div>
  );
}`,
    },
    vue: {
      language: 'vue',
      filename: 'InfiniteScrollLoader.vue',
      code: `<template>
  <div class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
    <p class="text-sm text-gray-700 dark:text-gray-300">Loading indicator for infinite scroll with pulse animation and scroll detection.</p>
    <button
      @click="isActive = !isActive"
      class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600"
    >
      Toggle State
    </button>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue';
const isActive = ref(false);
</script>`,
    },
    angular: {
      language: 'ts',
      filename: 'infinite-scroll-loader.component.ts',
      code: `import { Component } from '@angular/core';

@Component({
  selector: 'app-infinite-scroll-loader',
  template: \`
    <div class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
      <p class="text-sm text-gray-700 dark:text-gray-300">Loading indicator for infinite scroll with pulse animation and scroll detection.</p>
      <button (click)="toggle()"
        class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
        Toggle State
      </button>
    </div>
  \`
})
export class InfiniteScrollLoaderComponent {
  isActive = false;
  toggle() { this.isActive = !this.isActive; }
}`,
    },
    html: {
      language: 'html',
      filename: 'infinite-scroll-loader.html',
      code: `<!-- HTML + Alpine.js -->
<div x-data="{ isActive: false }" class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
  <p class="text-sm text-gray-700 dark:text-gray-300">Loading indicator for infinite scroll with pulse animation and scroll detection.</p>
  <button
    @click="isActive = !isActive"
    class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
    Toggle State
  </button>
</div>`,
    },
    laravel: {
      language: 'blade',
      filename: 'infinite-scroll-loader.blade.php',
      code: `<!-- Laravel Blade + Alpine.js -->
<div x-data="{ isActive: false }" class="rounded-lg border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-800">
  <p class="text-sm text-gray-700 dark:text-gray-300">Loading indicator for infinite scroll with pulse animation and scroll detection.</p>
  <button
    @click="isActive = !isActive"
    class="mt-3 rounded-md bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600">
    Toggle State
  </button>
</div>`,
    },
  },
};
